# fallback — placeholder

Future home of the Next.js 16 fallback frontend (the "if cal.diy goes
down, here's a static page" surface, plus any branded marketing pages
that sit alongside the cal.diy booking flow).

When you build it: scaffold with `pnpm create next-app@latest` here. It
does **not** consume the Python packages — keep it isolated.
